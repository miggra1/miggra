export default function NotesLoading() {
  return (
    <main className="min-h-screen bg-[var(--bg)] px-6 py-12 text-[var(--fg)]">
      <p className="mx-auto max-w-5xl text-sm text-[var(--muted)]">正在加载归档…</p>
    </main>
  );
}
